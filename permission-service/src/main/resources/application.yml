server:
  port: 8082

spring:
  application:
    name: permission-service

  datasource:
    url: jdbc:mysql://39.107.70.37:3306/permission_db?serverTimezone=UTC&useSSL=false
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis-plus:
  mapper-locations: classpath:mapper/*.xml
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
